# Cupid Toolchain capability matrix

Statuses in this initial matrix mean:

- **Observed**: the implementation is present in source and at least one relevant smoke was executed.
- **Partial**: useful implementation exists, but the required active-source or format coverage is not proven.
- **Missing**: no implementation satisfying the bootstrap contract was found.
- **Required**: the checked active-source audit proves the capability is exercised, but the Cupid implementation does not yet satisfy it.

The capability requirements below are backed by `ACTIVE-SOURCE-AUDIT.md` and `audits/active-build.json`: 681 active language inputs across root, user, and hosted toolchain build roots, 250 stable feature IDs, 490 output transforms, and an explicit i386 ILP32/cdecl/ELF32 contract. The language split is 26 assembly files, 267 C translation units, 262 C headers, and 126 Cupid C files. Lexical counts are discovery evidence; semantic completion still requires focused compiler/assembler tests.

## CupidC

| Capability | Status | Baseline evidence and gap |
| --- | --- | --- |
| In-OS JIT compilation and execution | Observed | Explicit `/bin/ls.cc` GUI smoke passed; the default `ls` smoke was flaky once before passing unchanged. |
| In-OS AOT executable output | Observed/Partial | Writer emits fixed-address ELF32 `ET_EXEC` code/data segments. `ccc /bin/ls.cc -o /home/cctest` produced an image that `exec` loaded, ran, and reaped without panic; systematic executable parity remains open. |
| C and Cupid language surface | Required/Partial | The audit covers 267 C translation units, 262 headers, and 126 Cupid C files. Required C includes structs/unions/enums, callbacks, bit-fields, static assertions, 64-bit integers, float/double, variadics, designated/partial initialization, and full control/expression semantics. Cupid mode additionally exercises sized types, `float4`/`double2`, class/new/del, reg/noreg, `#exe`, native asm blocks, and production browser-scale globals/includes. Current parser support is useful but not conformance-complete. |
| Preprocessing and includes | Required/Partial | Active tracked source uses exactly 2,112 direct quoted and 202 direct angle includes across 651 C-family inputs, zero macro operands, forced Doom compatibility headers, nested conditionals, recursive guarded includes, 194 function-like definitions, 13 variadic definitions, nine stringify operators, 13 paste operators including six GNU comma elisions, `__FILE__`/`__LINE__`, zero named `#line` directives or GNU numeric markers, one once pragma, four pack pragmas, and one Cupid `#exe` block. The freestanding one-operation module proves translation phases, ordered configured/object/function/variadic macros, stringify/paste/placemarkers, C11 conditionals and reproducible predefined macros, macro-expanded standard `#line`/`%:line` with dual presumed/physical provenance, direct and third-form macro-expanded include operands, forced and recursive cache-aware traversal, canonical once identity, translation-unit pack state/metadata, and policy-neutral typed `#exe` markers with transactional diagnostics and rollback. Expanded angle operands preserve surviving separation exactly. A checked Make-derived manifest runs that unchanged operation across all 356 tracked roots under seven exact profiles: 152 strict kernel, six Doom compatibility, 80 Doom-tree, three user, 104 Cupid programs, ten 64-bit hosted Toolchain units, and one 64-bit hosted kernel bridge. Each translation unit receives a fresh bounded job; a separate non-skipping target covers all four generated kernel roots. The hosted profiles preserve the exact common and bridge include-root sets, request hosted strict C11 semantics, and supply `__SIZEOF_POINTER__=8` for the current supported bootstrap hosts. The same contract records 22 browser fragments under their owner, two delivered headers with no invented standalone context, and 17 external-runtime/header hosted deferrals with zero hermetic deferrals. Profile values come from GNU Make evaluation, while target-required pointer/endian/SSE facts are explicit source-driven actions; `__GNUC__=1` means compatible definedness, not a host version. Named GNU variadics, digit-led line-marker semantics, external hosted-runtime/header integration, and kernel integration remain. |
| Typed-tape declaration frontend | Required/Partial | One freestanding `ctool_c_parse` operation consumes the immutable preprocessing tape and publishes a job-owned ADR 0013 graph plus completed layouts, canonical ordinary bindings, tags, normalized parameters, first-declaration storage/provenance, effective linkage, names, and dual presumed/physical locations. The unchanged 710-token `/kernel/fs/fat16.h` closure still exactly reproduces all FAT layouts. Four additional unchanged `KERNEL_I386` oracles pin `kernel.h` at 569 tokens/69 bindings, `irq.h` at 512/57, `cupidscript.h` at 1,867/177, and `shell.h` at 591/74, including one canonical binding at its first declaration for `print`, `irq_handler_t`, `script_context_t`, and `shell_jit_program_is_running`. Semantic GNU `packed`, `aligned`, and `noreturn` support covers active record/member/typedef/entity/function placements and pins unchanged process, per-CPU-prefix, IDT, loader, E1000, and panic ABI/effect oracles. File/record `_Static_assert`, all six integer comparisons, and target `sizeof(type-name)` now prove the unchanged `percpu.h:36` assertion and `per_cpu_t` at `128/64`. The active non-Doom header gate remains 146/152: the three SMP failures advance to `percpu.h:42`, so all six exact failures are inline definitions. Compatible objects/functions merge through iterative memoized type relations and symbol-local immutable composite types, with transparent aligned-wrapper compatibility and strongest-alignment retention. Contracts cover aligned incomplete/fixed arrays, opposing recursive alignment, qualifier placement and atomic minimum alignment in both declaration orders, incomplete/fixed arrays without typedef alias corruption, qualifier normalization including qualified array typedefs and retained atomic parameter identity, strict same-type typedef redefinition, old-style/prototype default promotions, opposing function-parameter composites, ordered internal/external linkage, precise attribute/assertion diagnostics, a 24-level repeated-pair compatibility DAG, 512 derived pointer levels beyond the source-syntax limit, and constrained composite/aligned-wrapper/layout-snapshot rollback. Ordinary/tag/member lookup remains linear, parameter storage and tentative-definition state/finalization are not yet published, and the expression grammar remains a deliberate subset. Function bodies/initializers, block assertions, `sizeof(expression)`, logical/conditional operators, remaining GNU attributes, the remaining Cupid extensions, typed AST/IR, code generation, and production integration remain. Of 22 active assertions across four files, only the `percpu.h` assertion is proven. |
| Immutable type graph and i386 object layout | Required/Partial | One freestanding `ctool_c_layout_types` operation consumes immutable indexed type/member/parameter arrays and publishes stable, request-ordered job-owned layouts. It fixes signed-char ILP32 scalar, pointer, vector, function-marker, aligned-wrapper, qualified-wrapper, array, struct, union, class, bit-field, flexible-array, packed, and explicit-alignment representation to the Cupid target. Enum representation is deliberately indirect: size, alignment, and signedness copy the frontend-selected compatible integer type. Manual active-source fixtures select signed `int`; the source-driven frontend selects unsigned `int` for both nonnegative FAT-closure enums and signed `int` or signed `long long` for contracted mixed cases. Separate positive contracts pin a synthetic signed-`long long`-compatible atomic enum at `8/8`, a direct atomic pointer at `4/4`, and an exact-16 aligned incomplete array at size zero with incomplete-object state. `QUALIFIED` preserves representation while giving a nominal type a distinct qualified identity without cloning a record slice; this keeps `const T *` distinct from `T * const`. Atomic `long long`/`double` have an eight-byte target minimum. Atomic identity propagates through `QUALIFIED` and `ALIGNED`, but the minimum is applied only when a node introduces `_Atomic`; a later exact `ALIGNED` wrapper may lower alignment, and a later non-atomic `QUALIFIED` wrapper copies it unchanged. Aggregate atomics introduced through either wrapper remain explicit unsupported input. GCC/Clang oracles pin aligned `int` `1 -> atomic 4 -> realigned 1` and aligned `long long` `1 -> atomic 8 -> realigned 1`. An iterative strong-edge walk handles arbitrary graph order and diagnoses by-value cycles without host recursion. Cached per-type `_Bool` identity, active-atomic state, and atomic-minimum facts keep bit-field/member work constant and the operation `O(types + members)`; the scale contract chains 4,096 wrappers above `_Bool` and lays out 4,096 fields twice. Per-member pack caps come from the first declaration-specifier token. A non-atomic aligned wrapper preserves the exact effective typedef/type-attribute alignment and may lower or raise the wrapped type; explicit record alignment only raises its computed result. Layout owns final-structure placement for a flexible array, while the frontend owns the more-than-one-named-member rule, including promoted names from anonymous records. Independent manual contracts pin all 54 FAT16 member offsets and active Doom/process/syscall/`e1000_rx_desc_t`/per-CPU shapes; the declaration frontend now reproduces the FAT oracles from unchanged source. This is still hosted compatibility evidence, not production ownership. |
| Type checking and ABI | Required/Partial | The contract is i386 ILP32, signed plain char, cdecl, 16-byte stack/SIMD alignment, and ELF32 `ET_REL`. Scalar and aggregate layout plus compatible declaration types/linkage now have shared hosted semantic operations. Active code still requires complete declaration coverage, tentative/initialized/defined entity state, expression and statement checking, float/double calls/returns, aggregate calling conventions, callbacks, weak/undefined symbols, and volatile MMIO/SMP behavior. Cupid mode's existing compact model is insufficient for C mode until these are proven. |
| Diagnostics | Partial | The shared preprocessing and declaration operations carry effective presumed and immutable physical locations. Lexical failures remain physical; preprocessing and declaration failures are transactional, preserve precise structured diagnostics, rewind operation allocations, and support same-job recovery. Declaration contracts cover syntax, namespace, legality, overflow, unsupported constructs, limits, and nested-layout failure. The private compiler still has `CC_MAX_ERRORS` one, and statement/type/backend diagnostic coverage remains incomplete. |
| Typed AST and linear IR | Missing | Current production implementation emits machine code during parsing. The shared preprocessing tape, immutable declaration graph, and i386 layout are parser-neutral semantic layers, but no statement/expression typed AST or linear IR exists yet. |
| Deterministic ELF32 `ET_REL` output | Missing | The shared writer now owns the required object model, but CupidC still emits fixed-address executables and has no semantic lowering into it. |
| Host-runnable compiler | Missing | CupidC is linked into the kernel and depends on kernel/VFS/runtime services. |
| Compile CupidC and the remaining toolchain | Missing | No staged self-compilation gate or checked-in seed exists. |
| GNU platform compatibility | Required/Partial | Active tracked C has 207 GNU basic/extended asm sites across 36 files and 71 real `__attribute__` sites across 30 files, containing 97 individual attribute-name occurrences: packed 30, aligned 12, noreturn 3, section 2, weak 5, used 18, noinline 18, unused 5, naked 3, and target 1. The audit scanner now handles multiline lists and no longer misses `ksyms.c`'s weak/section/aligned group. CupidC semantically supports packed, aligned, and noreturn at the declaration seam; all remaining names and extended assembly still block the active `.c` cohort. These are platform requirements, not optional source simplifications. |
| Doom compatibility mode | Required/Missing | The 83-file Doom/port C cohort needs old/no-prototype declarations, five implicit calls, permissive callback/object-pointer conversions, compatibility headers, and relaxed diagnostics without weakening strict C mode. |
| Optimization and measured quality gate | Missing | The oracle records size evidence, but no Cupid-built trusted cohort has the agreed size/runtime comparison yet. |

## CupidASM

| Capability | Status | Baseline evidence and gap |
| --- | --- | --- |
| In-OS JIT assembly and execution | Observed | The kernel `as` command now obtains its fixed image from the same `ctool_asm_assemble` operation as the hosted tool. A real `/demos/hello.asm` GUI-terminal smoke executed without panic, while the DEBUG boot self-test assembles `/demos/include_feature.asm` through VFS and checks the complete 631-definition runtime catalogue. Shell snapshots are region-aware so nested CupidC and CupidASM programs preserve their distinct fixed code/data regions. |
| In-OS AOT executable output | Observed/Partial | The shared assembler produces the fixed image and a checked buffer-only kernel bridge wraps code-only or code/data/BSS regions in sectionless i386 `ET_EXEC`. A real `cupidasm` hello build was loaded by `exec` as a process without panic. The bridge remains temporary compatibility ownership: final executable layout belongs to CupidLD. |
| Intel-style x86 encoder | Observed | The shared frontend consumes the one typed x86 model for every instruction. The 26 active assembly inputs contain 1,212 instruction statements, 90 mnemonic families plus two active prefixes, 27 register names, and 101 true memory operands; the audit correction excludes quoted strings and bracketed directives. Strict Windows Clang, Linux GCC, and ASan/UBSan contracts assemble all 22 demos plus the four production sources across mixed 16/32-bit, segment/control, far, ModRM/SIB, x87, and SSE2 forms. |
| Labels, expressions, and relocations | Observed | Colon and dot-local labels, `equ`, `$`/`$$`, label differences, checked arithmetic/shift/complement expressions, binary literals, deterministic branch relaxation, globals/externs, and absolute/PC-relative fixups are implemented. The real ISR and context-switch objects have semantic symbol/relocation parity, including `R_386_32` and `R_386_PC32`; positive and useful failure contracts cover undefined, duplicate, overflow, layout, and relocation cases. |
| Directives and source composition | Observed | The active surface is implemented: seven `BITS`, two `ORG`, sections, globals/externs, `db/dw/dd/dq`, `times`, `equ`, `%define`, `%include`, and all seven reserve spellings. Includes resolve from the including source parent and ordered logical roots with missing/cycle/depth diagnostics; the kernel orders those roots as shell CWD then `/`, yielding source parent, CWD, root lookup. No active source uses `%macro`; unrestricted macro support is not claimed. |
| Flat binary output for boot paths | Observed | Hosted CupidASM owns the production 2,560-byte boot image and 4,096-byte SMP trampoline, byte-identical to NASM with frozen SHA-256 evidence. A clean NASM-poisoned build plus four-CPU boot/runtime smoke proves both paths. |
| Deterministic ELF32 `ET_REL` output | Observed | CupidASM lowers sections, symbols, and fixups into the shared semantic ELF32 writer. The real ISR and context-switch sources match NASM's relevant section bytes, symbol bindings, and relocation semantics; deterministic and malformed/error contracts pass on both supported hosts. |
| Assemble all active OS assembly | Observed | All four unchanged production sources and all 22 unchanged embedded demos assemble through the shared frontend. The normal Make graph attributes the four production transforms to CupidASM and none to NASM; every demo produces the same fixed image on two runs with the exact required definition names/kinds and deterministic test addresses. The kernel adapter exercises the real 631-name case-insensitive catalogue, includes through VFS, JIT execution, and AOT execution. |
| Host-runnable assembler and self-build | Observed/Partial | `make -C toolchain all` builds a native `cupidasm -f bin|elf32 INPUT -o OUTPUT` CLI on Windows and Linux with distinct usage and processing failures. It is still compiled by GCC/Clang; no checked seed or staged self-build gate exists. |

## CupidDis

| Capability | Status | Baseline evidence and gap |
| --- | --- | --- |
| In-OS instruction decoding | Observed/Partial | The kernel `dis`, `exec -d`, and CupidC JIT adapters now call the same typed 16/32-bit x86 decoder and streaming formatter as hosted CupidDis; the duplicate kernel opcode tables are gone. The DEBUG boot self-test enters through both the preserved raw/JIT `dis_disassemble` adapter and a temporary-file VFS `dis_elf` command path rather than bypassing them. Contracts cover raw 16-bit/32-bit decoding, labels, 16-bit relative wraparound, unknown/invalid progress, and truncated tails across the active x87/SSE2-capable catalogue. A raw request has one mode, so mixed-mode flat images still need range extraction; completion also requires the remainder of the bounded ADR 0005 instruction domain and source annotations. |
| ELF32 executable inspection | Observed/Partial | The shared reader exposes checked static i386 `ET_EXEC` header and program-header views, including sectionless CupidC/CupidASM executables and sectioned CupidLD output. CupidDis selects executable `PT_LOAD` contents, preserves absolute symbols when sections exist, reports `PT_GNU_STACK`, and reports merge/string flags on inputs that truthfully retain them. CupidLD treats those flags as input link semantics and clears them on final allocated output sections because its combined executable-section model has no single truthful entry size. Dynamic symbol tables/relocations are diagnosed as unsupported until the typed model represents multiple symbol-table domains. |
| ELF32 `ET_REL`, sections, symbols, and relocations | Observed | Hosted and in-process CupidDis reports serialized headers, sections, local/global/weak/undefined, TLS, and unnamed section symbols, and serialized-order relocations from the shared reader, disassembles every executable `PROGBITS` section, and overlays matching `R_386_32`/`R_386_PC32` fields without assigning relocation ownership to raw bytes. View-gated sorted indexes keep label traversal linear, make relocation lookup logarithmic, and never span caller callbacks. Positive, malformed, ordering, overflow, report-shape, exact-diagnostic, and early/late sink-failure contracts cover the typed seam. Native hosted contract objects remain deliberately classified separately from i386 OS objects. |
| DWARF v4 source information | Missing | No line/debug information parser or source annotation is present. |
| Host-runnable object inspector | Observed/Partial | `make -C toolchain all` builds a native `cupiddis` CLI on Windows and Linux. Static ELF input defaults to all implemented views; explicit header/section/symbol/relocation/disassembly views and an address-sorted GNU-`nm`-compatible mode are available. Raw input deliberately requires explicit 16/32-bit mode and base. Buffered stdout failures are detected at flush and produce a processing failure. The executable is still bootstrapped by the host C compiler and lacks dynamic-ELF, raw range/mode-map, and DWARF views. |
| Encoder/decoder parity tests | Observed/Partial | One catalogue now drives CupidASM and CupidDis plus both encoding directions. Checked manifests cover 185 active-ASM mode cases and 129 active-inline source-spelling/form rows; 153 additional focused/current-CupidC vectors exercise model boundaries. Every manifest vector decodes and re-encodes with its fingerprint-scoped form to identical bytes, and the assembler adds source-to-artifact parity over all 26 active files. Negative and every-byte truncation cases cover illegal prefixes/groups/registers, reserved PSRLW memory encoding, range failures, and transactional output. A generated every-form exhaustive corpus remains. |

## Shared object, linker, and bootstrap capabilities

| Capability | Status | Baseline evidence and gap |
| --- | --- | --- |
| Platform-neutral job, arena, buffer, path, source, and diagnostics core | Observed/Partial | One freestanding implementation passes hosted public-contract tests and compiles into the i386 kernel through real libc and heap/VFS adapters. DEBUG boot self-tests exercise checked invocation, allocation/path/output behavior, VFS commit/cleanup, missing-input translation, and real CupidASM include resolution. Hosted and kernel CupidDis/CupidASM, hosted CupidObj/CupidLD, and the hosted CupidC preprocessing, declaration, and type/layout modules consume the job interface. Declaration and layout publish stable job-owned arrays, retain diagnostics, and rewind complete operation output on failure. The production kernel CupidC frontend has not migrated to these seams. |
| Shared ELF32 object library | Observed/Partial | One freestanding two-operation module writes deterministic i386 `ET_REL` objects and reads bounded typed static `ET_REL`/`ET_EXEC` views. Public contracts cover named/unnamed sections, byte-aligned and zero-count tables, symbols, weak/common storage, alignments, program headers, sectionless executables, both required relocations, explicit/implicit addends, determinism, rollback, malformed input, and an explicit unsupported diagnostic for dynamic symbol tables; GNU `readelf`, LLVM `ld.lld`, Clang, and the real NASM ISR object passed interoperability oracles. Kernel round-trip, CupidDis, CupidObj, and CupidLD exercise the same typed reader/writer seam. Multiple/dynamic symbol-table domains remain. |
| CupidLD | Observed | One freestanding transactional operation links ordered static i386 `ET_REL` inputs using either the exact `link.ld` subset or a fixed-text profile. It owns strong/weak/common/script symbols, `R_386_32`/`R_386_PC32`, allocated-section selection, BSS, five permission-separated `PT_LOAD` segments, `PT_GNU_STACK`, symbol/string tables, assertions, and checked rollback. Compatible unrelocated merge atoms intern in stable first-occurrence order; any atom overlapped by a relocation stays distinct. COMMON storage aligns from its absolute output address, and unsupported allocated section types or INIT_ARRAY/FINI_ARRAY/TLS/EXCLUDE semantics fail explicitly. The native CLI owns both kernel passes and three user executables; all 423 pass-one and 424 final objects link with host-oracle section/segment layout and symbol-set parity. Merge groups deliberately avoid LLVM LLD's undocumented hash-table order, so payload bytes and address fixups can differ while semantic layout remains equivalent. The executable is still built by GCC/Clang. |
| CupidObj | Observed | One freestanding transactional operation emits deterministic final-form i386 `ET_REL` binary wrappers or extracts initialized file-backed bytes from static i386 executables. Contracts cover exact names/flags/alignment/symbols, empty inputs, deterministic output, `PT_LOAD` ordering, physical-address gaps, allocated-section fallback, BSS exclusion, overlap/overflow/limit failures, and rollback. Its native CLI owns 178 direct wrappers, one JPEG-preprocessed wrapper, the final-form read-only SMP wrapper, and final kernel flattening in the normal build; the rebuilt kernel ELF, raw binary, and disk image retained their exact pre-cutover SHA-256 values and passed the GUI/JIT smoke. The hosted executable is still built by GCC/Clang. |
| Shared x86 instruction model | Observed | One freestanding typed 16/32-bit seam and one private flat catalogue serve encoding and decoding: 546 rows (544 encodable forms plus two decode-only invalid recognizers), 226 canonical mnemonics, 64 registers, and fingerprint `3159218E`. Contracts cover deterministic shortest-form selection, exact-form replay, fields suitable for absolute/PC-relative relocation, mixed addressing, far/system/x87/SSE2 instructions, typed MMX-ready operands/registers, conservative invalid/unknown/truncated decoding, and transactional failures. CupidASM and CupidDis consume the model; CupidC migration remains. |
| Windows/Linux oracle baseline | Observed | Checked schema-v2 evidence at `1e079d1` independently reproduces 431 root, three user, and thirteen hosted artifacts on canonical Windows Clang/LLVM and Linux GCC/binutils hosts. Run one on each host passes the host suite and CupidC/CupidASM guest smokes; both runs pass all three root build/list gates, and the checked cross-host gate validates source/tool/capability/manifests/quality while correctly allowing platform suffixes and host-compiler bytes to differ. This is oracle evidence, not a checked Cupid seed or self-hosting stage. |
| Windows and Linux checked seeds | Missing | No checked-in host-runnable Cupid Toolchain executables or provenance manifests exist. |
| Stage 2/stage 3 byte identity | Missing | No deterministic staged bootstrap harness exists. |
| Clean-checkout host independence | Missing | An external compiler plus its native linker backend and `nm` are still required. NASM, standalone host ELF-linker recipes, and `objcopy` have left OS/user artifact production. |
