# Run the canonical bootstrap tools on Windows and Linux

The canonical Cupid Toolchain will be host-runnable on both Windows and Linux, while Make and Python may remain as build orchestration and image-packaging dependencies. This keeps a fresh-checkout bootstrap accessible on supported development hosts without retaining external compilers, assemblers, linkers, or binary utilities in the normal build.
