# Run the canonical bootstrap tools on Windows and Linux

The canonical Cupid Toolchain will run on both Windows and Linux. Make and Python may remain as build orchestration and image-packaging dependencies. A fresh checkout can bootstrap on either supported host without external compilers, assemblers, linkers, or binary utilities in the normal build.
